     How to use the /obj/newboard object
--------------------------------------------
An example piece of code:

reset(arg)
{
  object board;
  if(arg)
    return;
  board=clone_object("obj/newboard");
  board->set_name("imagers");
  move_object(board,this_object());
}


